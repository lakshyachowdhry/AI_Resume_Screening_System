version: '3.8'

services:
  ai-resume-screening:
    build: .
    ports:
      - "8501:8501"
    environment:
      - GEMINI_API_KEY=${GEMINI_API_KEY}
    volumes:
      - ./model:/app/model
      - ./outputs:/app/outputs
    restart: unless-stopped